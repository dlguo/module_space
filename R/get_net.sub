#!/bin/sh -l

me=`basename "$0"`
#PBS -l nodes=1:ppn=1
#PBS -l walltime=10:00:00
#PBS -o ../output/${me}.o
#PBS -e ../output/${me}.e

module load r
cd $PBS_O_WORKDIR 

R --vanilla --no-save < main.R 
