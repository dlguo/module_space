#!/bin/sh -l

#PBS -l nodes=1:ppn=1
#PBS -l walltime=4:00:00
#PBS -l naccesspolicy=shared
#PBS -o ../output/msg/${SUBJ}_${SESS}.o
#PBS -e ../output/msg/${SUBJ}_${SESS}.e

module load r
cd $PBS_O_WORKDIR 

R --vanilla --no-save --args < get_nets.R 90 .4 ${SUBJ} ${SESS}
